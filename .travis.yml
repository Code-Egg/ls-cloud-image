language: shell
os: linux

notifications:
  email:
    on_success: never
    on_failure: always

script:
  #- wget https://raw.githubusercontent.com/litespeedtech/ls-cloud-image/master/Setup/wpimgsetup.sh 
  #- sudo chmod +x wpimgsetup.sh 
  #- sudo bash -x wpimgsetup.sh
  - cd /tmp/; wget -q https://raw.githubusercontent.com/litespeedtech/ols1clk/master/ols1clk.sh
  - sudo chmod +x ols1clk.sh
  - echo 'Y' | sudo bash ols1clk.sh --lsphp 73 --wordpress
    #--wordpresspath ${DOCHM} \
    #--dbrootpassword ${root_mysql_pass} \
    #--dbname wordpress \
    #--dbuser wordpress \
    #--dbpassword wordpress

